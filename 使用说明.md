# ComfyUI 启动器 使用说明

## 概述

ComfyUI 启动器是一个为 ComfyUI 设计的桌面管理工具（Windows，PyQt5）。
它把 ComfyUI 的启动参数、环境、插件、内核版本、外置模型库等分散的配置，
集中到一个图形界面里管理，并提供内置的 ComfyUI 实时日志和多环境切换。

启动器本身**不修改 ComfyUI 的核心代码**，只负责帮你拼参数、起进程、管配置。

---

## 界面总览

主界面是「左侧导航栏 + 右侧内容区」的结构。左侧从上到下依次是：

| 导航项 | 作用 |
|---|---|
| 🚀 启动与更新 | 启动/停止 ComfyUI、配置启动参数、查看版本、更新内核 |
| 🧪 WebUI 工作台 | （可选）管理多工作流 WebUI 工作台，与 ComfyUI 平级 |
| 📋 ComfyUI 实时日志 | 实时显示 ComfyUI 运行日志 |
| 🧩 插件管理 | 管理 `custom_nodes` 插件：增删、启用/禁用、检查与批量更新 |
| 🧬 内核版本管理 | 查看 / 更新 / 回退 ComfyUI 内核到任意历史提交 |
| 📂 外置模型库管理 | 管理 `extra_model_paths.yaml`，挂载散落在各处的模型目录 |
| 📋 后台任务 | 查看正在运行和已完成的后台任务（更新、检查等） |
| ⚙️ 系统设置 | 环境管理、窗口与托盘行为 |
| 👤 关于我 / 📚 关于 ComfyUI / 🧰 关于启动器 | 作者信息、相关链接、启动器自更新 |

导航栏底部有两个按钮：**🌙 深色 / ☀️ 浅色** 切换主题（切换后会重启启动器生效），
以及 **◀ / ▶** 折叠/展开侧边栏。

---

## 多环境（核心概念）

**一个环境 = 一个 ComfyUI 根目录 + 一个 Python 路径** 的组合。
启动器支持保存多组环境，在「系统设置 → 环境管理」里新增 / 编辑 / 删除，
在「启动与更新」页面顶部的下拉框快速切换当前激活环境。

切换规则（为了避免操作错仓库）：

- **同时只能运行一个环境**。切换时如果 ComfyUI 还在跑，会提示先停止。
- **有后台任务进行中时禁止切换**（比如正在更新内核 / 插件），等任务完成后再切。
- 切换后，版本、插件、模型、日志等页面会自动刷新到新环境。

---

## 🚀 启动与更新

这是最常用的页面。从上到下分几块：

### 顶部：环境选择栏
显示「当前环境」下拉框 + 根目录 / Python 路径摘要 + 「管理环境」按钮。

### 启动控制
一组表单，**改动会自动保存**，无需点保存按钮：

- **运行模式**：GPU / CPU
- **端口号**：默认 8188；勾选「允许局域网访问」可让同一网络的其他设备访问
- **显存策略**：由 ComfyUI 决定（推荐）/ 高 / 中 / 低 / 极低
- **注意力优化**：SDPA / Flash / Sage / Split / Quad 等
- **显卡**：自动列出本机 GPU（CPU 模式下禁用）
- **自动打开浏览器**：不打开 / 默认浏览器 / 指定浏览器（可选 `.exe`）
- **勾选项**：快速 FP16 累加、禁用 API 节点、禁用所有插件（DEBUG）、显示命令行窗口
- **额外选项**：自由填写额外的启动参数，例如 `--disable-smart-memory --fp16-vae`
- **环境变量**：自由填写 `KEY=VALUE`，逗号分隔

### 大按钮区
- **🚀 一键启动**：启动 ComfyUI；运行中再次点击 = 停止（按钮文字会变成「停止」）
- **常见问题**：打开飞书知识库
- **打包日志**：把启动器 + ComfyUI 日志打包成 zip，方便反馈问题

### 环境配置（镜像 / 代理）
- **HF 镜像源**：默认 `https://hf-mirror.com`，加速 HuggingFace 模型下载
- **GitHub 代理**：默认 `https://gh-proxy.com/`，加速 git 克隆
- **PyPI 代理**：阿里云 / 清华 / 华为云 任选，加速 pip 安装

### 版本与更新
显示 7 个组件版本卡片（内核 / 前端 / 模板库 / Python / Torch / Git / 显卡驱动）。
可设「仅更新到稳定版」「同时更新依赖库」和更新超时，点「更新」更新内核，「刷新」重取版本号。

### 快捷目录
8 个按钮直接在资源管理器打开：根目录 / ComfyUI 日志 / 启动器日志 / 输出 / 输入 / 插件 / 工作流 / 模型。

---

## 🧪 WebUI 工作台（可选）

管理一个独立的多工作流 WebUI 应用（Comfyui-Workbench-Mie），与 ComfyUI 主服务**平级运行**。
默认端口 **8199**，首次使用需要先下载安装。

页面根据安装状态自动切换大按钮文字：

| 状态 | 按钮文字 |
|---|---|
| 未安装 | ⬇ 下载WebUI工作台 |
| 缺依赖 | ⚙ 安装依赖 |
| 就绪 | 🚀 一键启动 |
| 运行中 | ⏹ 停止 |
| 操作中 | ⏳ …中（禁用） |

> 注意：启动 WebUI 工作台前会检查 ComfyUI 是否在跑，没在跑会等待它先启动。
> 「移除」按钮会删除 WebUI 工作台目录，需先在未运行状态操作。

---

## 📋 ComfyUI 实时日志

实时跟踪 ComfyUI 输出。工具栏：

- **暂停 / 继续**：暂停滚动查看
- **清空**：只清空当前显示（不改原日志文件）
- **保存为...**：把当前内容导出
- **📁 打开日志文件**：用系统编辑器打开原始日志

---

## 🧩 插件管理

管理 `ComfyUI/custom_nodes` 下的插件。

- **顶部**：刷新列表、安装插件（输入 git URL 或 CNR id）、检查更新、更新全部
- **批量操作**：勾选插件后出现「更新选中 / 启用选中 / 禁用选中 / 卸载选中」
- **列表**：显示每个插件的更新状态、版本、远端日期、类型（Git / CNR / 本地）、启用状态

「检查更新」会把有更新的插件排到列表顶部。卸载和强制更新前会弹确认框。

---

## 🧬 内核版本管理

除了在启动页更新到最新版，这里可以**切换到任意历史提交**：

- **当前版本信息**：显示内核版本，可设 GitHub 代理和更新策略
- **提交历史**：分页表格（每页 50 条），列含提交哈希 / 日期 / 作者 / 提交信息
- 选中某行 + 点「切换到所选提交」= `git checkout` 到那个版本（ComfyUI 运行中会拒绝）

---

## 📂 外置模型库管理

管理 ComfyUI 的 `extra_model_paths.yaml`——把散落在各处的模型目录挂载进 ComfyUI，
不必把模型文件拷进 ComfyUI 自带目录。

- **全局操作**：刷新/应用更改、打开 YAML 文件、仅使用内置、恢复配置、添加库、移除所选
- **左侧库列表**：`★` 标记默认库，`(停用)` 标记已禁用
- **右侧库配置**：编辑名称、启用/禁用、设为默认库、打开目录
- **映射列表**：展示该库下的子目录与 ComfyUI 内部路径的映射关系

> 提示：在外置库目录下新建或删除子文件夹后，记得点「应用更改」重写 yaml。

---

## 📋 后台任务

内核更新、插件检查/更新、拉取提交历史等耗时操作都在后台跑，在这里查看进度。
分「进行中」和「已完成」两个标签页，点「显示」可找回进度弹窗。

---

## ⚙️ 系统设置

### 环境管理
新增 / 编辑 / 删除环境（至少保留一个）。每个环境填名称、ComfyUI 根目录、Python 路径。

### 窗口与托盘
- **关闭主窗口时最小化到系统托盘**：勾选后关窗口不退出，驻留托盘
- **每次关闭时都提醒**：每次关窗口都问一次是否最小化

托盘菜单提供：显示主窗口、查看 ComfyUI 状态、退出启动器、退出并关闭 ComfyUI。

---

## 系统托盘与退出

启动器可以驻留系统托盘（由「系统设置 → 窗口与托盘」控制）。

- 从托盘选「退出启动器」才会真正退出
- 选「退出并关闭 ComfyUI」会在退出时一并停掉 ComfyUI 服务

---

## 配置文件

| 文件 | 含义 |
|---|---|
| `launcher/config.json` | 启动器主配置：环境列表、启动参数、代理、模型库设置等 |
| `<comfyui_root>/user/comfyui.log` | ComfyUI 运行日志 |
| `launcher/launcher.log` | 启动器自身日志 |
| `launcher/comfyui.pid` | ComfyUI 进程协调文件（跨进程，自动管理） |

> 这些文件包含本机绝对路径，属于运行时本地文件，不要手动提交到版本库。

---

## 常见问题

| 现象 | 排查 |
|---|---|
| 点击「一键启动」没反应 | 检查当前环境路径是否正确（顶部环境栏）；看「ComfyUI 实时日志」页报什么错 |
| 启动后浏览器没打开 | 检查「自动打开浏览器」设置；或手动访问 `http://127.0.0.1:8188` |
| 端口冲突 | 改「端口号」；或用「系统设置」看是否有别的环境占用 |
| 更新内核失败 | 在「内核版本管理」页设 GitHub 代理；检查网络；确认 git 树没被手动改脏 |
| 模型下载慢 | 在「环境配置」里启用 HF 镜像源和 GitHub 代理 |
| 反馈问题 | 用「启动与更新」页的「打包日志」按钮生成日志 zip，附到 issue |

---

## 命令行 / 自动化

启动器同时支持 headless 命令行模式（适合开机自启、监控、脚本自动化），
与 GUI 共用同一套启动/停止逻辑。完整 CLI 参考见同目录的 [`cli.md`](cli.md)。

配合 `ComfyUI启动器-CLI.cmd`（与 `ComfyUI启动器.exe` 同目录）使用，
对监控脚本更友好：

```bash
ComfyUI启动器-CLI.cmd status --json    # 查状态
ComfyUI启动器-CLI.cmd start            # 启动
ComfyUI启动器-CLI.cmd stop             # 停止
ComfyUI启动器-CLI.cmd help             # 帮助
```
